# InfluxDB (neue, dedizierte Instanz)
INFLUX_URL=http://influxdb:8086
INFLUX_TOKEN=changeme-super-secret-token
INFLUX_ORG=energy
INFLUX_BUCKET=readings

# SQLite-Datei (Stammdaten)
SQLITE_PATH=./data/systems.db

# CORS (hinter Tailscale unkritisch)
CORS_ORIGINS=*
