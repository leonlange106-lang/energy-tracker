name: "Zählwerk"
version: "2.0.0"
slug: "energy_tracker"
description: "Zählwerk – Verbrauchs- & Zählerstands-Tracking (Strom, Gas, Wasser, PV). Standalone, nur SQLite."
url: "https://github.com/leonlange106-lang/energy-tracker"
arch:
  - amd64
init: false
# Ingress: WebUI direkt in HA-Sidebar + HA-App, ohne separaten Port/Login
ingress: true
ingress_port: 8000
panel_icon: "mdi:gauge"
panel_title: "Zählwerk"
# Keine Optionen mehr nötig – komplett standalone (SQLite in /data)
